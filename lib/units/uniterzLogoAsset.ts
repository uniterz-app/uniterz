/**
 * 確定版 UNITERZ ロゴ画像アセット（参考画像をそのまま採用）。
 * アスペクト比 1024×682。
 */
export const UNITERZ_LOGO_ASSET = {
  /** Web: `public/brand/uniterz-logo.png` */
  webPath: "/brand/uniterz-logo.png",
  width: 1024,
  height: 682,
  aspectRatio: 1024 / 682,
} as const;
