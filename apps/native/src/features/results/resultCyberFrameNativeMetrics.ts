/** Web 結果サイバー枠の RN 描画定数 */

/** 静的枠線（hit / 連勝 / upset / perfect） */
export const RESULT_CYBER_FRAME_STROKE_WIDTH = 3;

/** 角飾り・上部ラインの stroke */
export const RESULT_CYBER_FRAME_CORNER_STROKE_WIDTH = 3;

/** 走査光リング幅（Web `.result-card-border-sweep` padding 4px） */
export const RESULT_CYBER_FRAME_SWEEP_PADDING = 4;

/** Predict オーバーレイ — 静的枠の上に載る走査光 */
export const PREDICT_OVERLAY_SWEEP_RING_WIDTH = 4;

/** halo 用 — コアリングより内側へ広げる分（inner inset をこの px 分だけ浅く） */
export const SWEEP_GLOW_RING_EXTRA = 3;
