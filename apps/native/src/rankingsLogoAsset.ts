/**
 * ランキング背景用 GLB。`apps/native/assets` への require をここに集約（深い `../../../` の誤りを防ぐ）。
 */
// eslint-disable-next-line @typescript-eslint/no-require-imports
export const UNITERZ_LOGO_GLB_ASSET = require("../assets/models/uniterz-logo.glb");
